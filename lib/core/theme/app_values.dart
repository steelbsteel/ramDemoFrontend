abstract final class AppValues{
  /// Duration
  static const Duration routeTransitionDuration = Duration(milliseconds: 400);
  static const Duration queryTimeoutDuration = Duration(seconds: 15);

  /// Routes
  static const String wrapperInitPath = '';
  static const String splashPath = "/";
}